{
  "name": "node-chat-app",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && nodemon --exec 'mocha server/**/*.test.js'",
    "start": "node server/server.js",
    "watch": "nodemon server/server.js"
  },
  "engines": {
    "node": "7.0.0"
  },
  "keywords": [],
  "author": "Carlo Rizzante <hello@carlorizzante.com> (http://carlorizzante.com)",
  "license": "MIT",
  "dependencies": {
    "express": "^4.14.0",
    "hbs": "^4.0.1",
    "moment": "^2.17.0",
    "socket.io": "^1.6.0"
  },
  "devDependencies": {
    "expect": "^1.20.2",
    "mocha": "^3.1.2",
    "nodemon": "^1.11.0"
  }
}
